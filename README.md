Build Setup

```
# install dependencies
npm install

# serve with hot reload 
npm run serve

# config `VUE_APP_URL=http://hostip:8081/cms/` in file `.env.prod`
# build for production with minification
npm run build:prod

```

